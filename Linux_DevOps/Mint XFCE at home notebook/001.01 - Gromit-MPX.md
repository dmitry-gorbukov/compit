
https://chat.deepseek.com/share/ij0c331jd9qiwnlphc

Отлично! **Gromit-MPX успешно установлен** и работает. Теперь давайте настроим его правильно.

## **🚨 ЧТО ПРОИЗОШЛО:**
1. ✅ Программа установлена (версия 1.5.1)
2. ⚠️ Нет пользовательского конфига (используется системный)
3. ⚠️ Горячие клавиши по умолчанию: `Home` и `End`

---

## **🔧 НАСТРОЙКА ГОРЯЧИХ КЛАВИШ**

### **1. Создайте пользовательский конфиг:**
```bash
# Создайте папку если нет
mkdir -p ~/.config/gromit-mpx

# Создайте конфиг файл
nano ~/.config/gromit-mpx/gromit-mpx.cfg
```

### **2. Вставьте этот конфиг (оптимизированный для XFCE):**
```ini
[General]
ShowTrayIcon=true
StartHidden=true
DebugMode=false

# ВАЖНО: Отключаем стандартные Home/End
UseBuiltinShortcuts=false

[Brushes]
# Формат: имя|цвет;толщина
brush1=red Pen|#FF0000;5
brush2=blue Pen|#0000FF;5  
brush3=yellow Pen|#FFFF00;5
brush4=green Marker|#00FF00;6
brush5=Eraser|#FFFFFF;25

[Shortcuts]
# Глобальные горячие клавиши (можно изменить)
Toggle=F9
Clear=Shift+F9
Undo=Ctrl+Z
Redo=Ctrl+Y
Visibility=Ctrl+F9
Quit=Ctrl+Q
```

Сохраните: `Ctrl+X` → `Y` → `Enter`

---

## **3. НАЗНАЧЬТЕ ГОРЯЧИЕ КЛАВИШИ В XFCE**

### **Способ 1: Через графический интерфейс (проще)**
```bash
# Откройте настройки клавиатуры
xfce4-keyboard-settings
```
Или: **Меню → Параметры → Клавиатура**

### **Способ 2: Через терминал (быстрее)**
```bash
# Удалите старые привязки Home/End если есть
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/Home" -r 2>/dev/null || true
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/End" -r 2>/dev/null || true

# Назначьте новые горячие клавиши
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/F9" -n -t string -s "gromit-mpx --toggle"
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Shift>F9" -n -t string -s "gromit-mpx --clear"
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Primary>F9" -n -t string -s "gromit-mpx --undo"
```

---

## **4. ЗАПУСТИТЕ И ПРОВЕРЬТЕ**

### **Запустите Gromit-MPX:**
```bash
# Если уже запущен - завершите
killall gromit-mpx 2>/dev/null || true

# Запустите снова
gromit-mpx &

# Проверьте иконку в трее (рядом с часами)
```

### **Быстрая проверка:**
```bash
# Проверьте конфиг
gromit-mpx --version

# Включите рисование
gromit-mpx --toggle
```

---

## **5. КАК РИСОВАТЬ (ИНСТРУКЦИЯ):**

1. **Нажмите `F9`** - включится режим рисования
2. **Рисуйте** левой кнопкой мыши
3. **Смените цвет/инструмент** - правая кнопка мыши
4. **Измените размер кисти** - колесо мыши
5. **Отмените линию** - `Ctrl+Z`
6. **Очистите экран** - `Shift+F9`
7. **Выйти из режима** - снова `F9`

---

## **6. ДОБАВЬТЕ В АВТОЗАГРУЗКУ**

```bash
# Создайте файл автозагрузки
mkdir -p ~/.config/autostart
cat > ~/.config/autostart/gromit-mpx.desktop << 'EOF'
[Desktop Entry]
Type=Application
Name=Gromit-MPX
Comment=Draw on the screen
Exec=gromit-mpx
Icon=gromit-mpx
Categories=Utility;
StartupNotify=false
Terminal=false
EOF
```

Или через GUI: **Меню → Параметры → Автозапуск** → Добавить `gromit-mpx`

---

## **7. ЕСЛИ КЛАВИШИ НЕ РАБОТАЮТ**

### **Проверьте текущие привязки:**
```bash
xfconf-query -c xfce4-keyboard-shortcuts -l | grep -E "(F9|Home|End)"
```

### **Если F9 уже занята:**
```bash
# Освободите F9
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/F9" -r

# Используйте другую комбинацию
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Super>d" -n -t string -s "gromit-mpx --toggle"
```

---

## **8. КОМАНДЫ ДЛЯ БЫСТРОГО ТЕСТА**

```bash
# Тест 1: Включить/выключить
gromit-mpx --toggle

# Тест 2: Рисуем красным
gromit-mpx --toggle
# (рисуйте мышью)
# Нажмите правую кнопку для смены цвета
# Еще раз --toggle для выхода

# Тест 3: Очистить
gromit-mpx --clear
```

---

## **🎯 ЧТО ДЕЛАТЬ ДАЛЬШЕ:**

1. **Нажмите `F9`** - должен появиться курсор-кисть
2. **Порисуйте** на рабочем столе
3. **Нажмите правую кнопку** - смените цвет
4. **Нажмите `Shift+F9`** - очистите
5. **Нажмите `F9`** - выйдите из режима

**Если всё работает** - добавьте в автозагрузку и наслаждайтесь рисованием поверх экрана! 🎨

**Если не работает** - напишите, какие именно горячие клавиши пробовали и что происходит.
<!--stackedit_data:
eyJoaXN0b3J5IjpbMTY2NzQ0NzA4NV19
-->