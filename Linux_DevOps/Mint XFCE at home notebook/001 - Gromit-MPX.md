Gromit-MPX (рекомендую для XFCE)
дай подробную инструкцию по установке.

-------------------
https://chat.deepseek.com/share/pf2hphrmzd2ubcw787

------------------------

# 📝 **Подробная инструкция по установке и настройке Gromit-MPX в Linux Mint XFCE**

## **1. ЧТО ТАКОЕ GROMIT-MPX?**
Легкая программа для рисования поверх экрана (как маркер на доске). Идеально для:
- 🎓 Проведения презентаций
- 💼 Объяснения материала на скриншотах  
- 🎨 Быстрых пометок на экране
- 📝 Аннотаций в реальном времени

---

## **2. ПОЛНАЯ УСТАНОВКА**

### **Шаг 1: Откройте терминал**
`Ctrl + Alt + T` или через меню → "Терминал"

### **Шаг 2: Обновите систему**
```bash
sudo apt update
sudo apt upgrade -y
```

### **Шаг 3: Установите Gromit-MPX**
```bash
sudo apt install gromit-mpx
```

### **Шаг 4: Проверьте установку**
```bash
# Проверьте версию
gromit-mpx --version

# Или просто запустите
gromit-mpx &
```
Вы увидите иконку в системном трее (рядом с часами).

---

## **3. БАЗОВАЯ НАСТРОЙКА**

### **Создайте конфигурационный файл:**
```bash
# Создайте папку для конфигов
mkdir -p ~/.config/gromit-mpx

# Создайте конфиг файл
nano ~/.config/gromit-mpx/gromit-mpx.cfg
```

### **Вставьте базовую конфигурацию:**
```ini
[General]
ShowTrayIcon=true
StartHidden=true
DebugMode=false

[Brushes]
# Формат: цвет;толщина
brush1=#FF0000;3      # Красный, толщина 3
brush2=#00FF00;5      # Зеленый, толщина 5  
brush3=#0000FF;7      # Синий, толщина 7
brush4=#FFFF00;10     # Желтый, толщина 10
brush5=#FF00FF;15     # Розовый, толщина 15

[Shortcuts]
# Глобальные горячие клавиши
Toggle=Ctrl+Super+D
Clear=Ctrl+Shift+Super+D
Quit=Ctrl+Super+Q
Undo=Ctrl+Super+Z
Redo=Ctrl+Super+Y
```
Сохраните: `Ctrl+X` → `Y` → `Enter`

---

## **4. НАСТРОЙКА ГОРЯЧИХ КЛАВИШ В XFCE**

### **Способ A: Через графический интерфейс**
1. Откройте **Меню → Параметры → Клавиатура**
2. Перейдите на вкладку **"Сочетания клавиш приложений"**
3. Нажмите **"Добавить"** и создайте:

**Основные комбинации:**
| Действие | Команда | Рекомендуемая клавиша |
|----------|---------|----------------------|
| Включить/выключить | `gromit-mpx --toggle` | `F9` |
| Очистить экран | `gromit-mpx --clear` | `Shift+F9` |
| Показать/скрыть | `gromit-mpx --visibility` | `Ctrl+F9` |
| Отменить (Undo) | `gromit-mpx --undo` | `Ctrl+Z` |

### **Способ B: Через терминал (более гибкий)**
```bash
# Установите глобальные горячие клавиши
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Primary><Super>d" -n -t string -s "gromit-mpx --toggle"
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Primary><Shift><Super>d" -n -t string -s "gromit-mpx --clear"
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Primary><Super>z" -n -t string -s "gromit-mpx --undo"

# Альтернатива - использовать F-клавиши
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/F9" -n -t string -s "gromit-mpx --toggle"
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/<Shift>F9" -n -t string -s "gromit-mpx --clear"
```

### **Способ C: Мой рекомендуемый набор**
Добавьте в `~/.config/xfce4/xfconf/xfce-perchannel-xml/xfce4-keyboard-shortcuts.xml`:
```xml
<!-- В раздел <channel name="xfce4-keyboard-shortcuts"> -->
<property name="F9" type="string" value="gromit-mpx --toggle"/>
<property name="Shift+F9" type="string" value="gromit-mpx --clear"/>
<property name="Ctrl+F9" type="string" value="gromit-mpx --undo"/>
<property name="<Super>d" type="string" value="gromit-mpx --toggle"/>
<property name="<Super><Shift>d" type="string" value="gromit-mpx --clear"/>
```

---

## **5. ДОБАВЛЕНИЕ В АВТОЗАГРУЗКУ**

### **Шаг 1: Откройте менеджер автозагрузки**
```bash
xfce4-autostart-editor
```
Или через меню: **Меню → Параметры → Автозапуск**

### **Шаг 2: Добавьте Gromit-MPX**
1. Нажмите **"Добавить"**
2. Заполните:
   - **Имя**: `Gromit-MPX`
   - **Описание**: `Рисование поверх экрана`
   - **Команда**: `gromit-mpx`
3. Нажмите **"ОК"**
4. Убедитесь, что галочка установлена

---

## **6. ПРОВЕРКА РАБОТЫ**

### **Тест 1: Быстрый запуск**
```bash
# Запустите программу
gromit-mpx &

# Включите режим рисования
gromit-mpx --toggle
```
На экране должна появиться круглая иконка-курсор.

### **Тест 2: Проверка горячих клавиш**
1. Нажмите вашу назначенную клавишу (например `F9`)
2. Попробуйте порисовать мышью
3. Смените цвет правой кнопкой мыши
4. Нажмите `F9` еще раз для выхода

### **Тест 3: Функции рисования**
- **Левая кнопка мыши** - рисовать
- **Правая кнопка мыши** - сменить цвет/кисть
- **Колесо мыши** - изменить размер кисти
- **Ctrl+Z** - отменить последнюю линию

---

## **7. РАСШИРЕННАЯ НАСТРОЙКА**

### **Кастомизация кистей:**
Отредактируйте `~/.config/gromit-mpx/gromit-mpx.cfg`:
```ini
[Brushes]
# Добавьте свои кисти
brush1=#FF5733;2      # Оранжевый, тонкий
brush2=#33FF57;8      # Зеленый, средний  
brush3=#3357FF;15     # Синий, толстый
brush4=#F333FF;20     # Фиолетовый, очень толстый
brush5=#000000;3      # Черный для текста
brush6=#FFFFFF;10     # Белый для выделения
```

### **Настройка прозрачности:**
```bash
# Запуск с прозрачностью 50%
gromit-mpx --opacity 0.5
```

### **Экспорт рисунков:**
```bash
# Сохранить текущий рисунок
gromit-mpx --export ~/Изображения/рисунок_$(date +%Y%m%d_%H%M%S).png
```

---

## **8. ПОЛЕЗНЫЕ КОМАНДЫ**

### **Основные команды:**
```bash
# Включить/выключить
gromit-mpx --toggle

# Очистить экран
gromit-mpx --clear

# Отменить последнее действие
gromit-mpx --undo

# Показать/скрыть интерфейс
gromit-mpx --visibility

# Выход из программы
gromit-mpx --quit

# Экспорт в PNG
gromit-mpx --export рисунок.png
```

### **Автоматизация:**
```bash
# Создайте скрипт для быстрого доступа
nano ~/.local/bin/draw.sh
```
Добавьте:
```bash
#!/bin/bash
# Скрипт для быстрого управления Gromit-MPX

case $1 in
    start)
        gromit-mpx &
        ;;
    toggle)
        gromit-mpx --toggle
        ;;
    clear)
        gromit-mpx --clear
        ;;
    save)
        gromit-mpx --export ~/Рисунки/$(date +"%Y%m%d_%H%M%S").png
        ;;
    *)
        echo "Использование: draw {start|toggle|clear|save}"
        ;;
esac
```
Сделайте исполняемым:
```bash
chmod +x ~/.local/bin/draw.sh
```

---

## **9. РЕШЕНИЕ ПРОБЛЕМ**

### **Проблема: Нет иконки в трее**
```bash
# Убедитесь, что статусная панель включена
# В конфиге: ShowTrayIcon=true

# Перезапустите
killall gromit-mpx
gromit-mpx &
```

### **Проблема: Горячие клавиши не работают**
```bash
# Проверьте, не заняты ли клавиши
xfconf-query -c xfce4-keyboard-shortcuts -l

# Освободите клавишу F9 если занята
xfconf-query -c xfce4-keyboard-shortcuts -p "/commands/custom/F9" -r
```

### **Проблема: Лагает рисование**
```bash
# Запустите с оптимизацией
gromit-mpx --redraw-interval 50
```

---

## **10. БЫСТРЫЙ СТАРТ (ЧИТ-ЛИСТ)**

1. **Установка**: `sudo apt install gromit-mpx`
2. **Запуск**: `gromit-mpx &`
3. **Горячие клавиши**: 
   - `F9` - начать/закончить рисование
   - `Shift+F9` - очистить экран
   - `Ctrl+Z` - отменить линию
4. **Рисование**: 
   - Левая кнопка - рисовать
   - Правая кнопка - сменить цвет
   - Колесо мыши - размер кисти
5. **Автозапуск**: Добавьте в "Автозапуск"

---

## **🎯 ИТОГ:**
1. **Установили** одной командой
2. **Настроили** горячие клавиши
3. **Добавили** в автозагрузку
4. **Теперь можете** рисовать поверх любого контента!

**Для проверки**: Нажмите `F9` и попробуйте что-нибудь нарисовать на рабочем столе. Все работает! ✨
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTE5Njg0MzE1MTldfQ==
-->