
Получил рекомендации:
"
💡 Рекомендация
Исходя из ваших требований (работа + личное, Ubuntu/Windows, аналог Confluence), я бы посоветовал начать со следующей связки:

Для хранения накопленных материалов (База Знаний): Развернуть Wiki.js.

Почему он: Самый современный, активное развитие, прекрасная документация, относительно простая установка на Ubuntu. Он станет вашим "единым источником правды" для всей документации по всем проектам.

Для управления задачами и планами: Использовать OpenProject или Trello.

OpenProject — если вам нужна серьезная система с календарями, диаграммами Ганта и глубоким планированием.

Trello — если вам больше подходит визуальный, гибкий канбан-подход.

Для быстрых личных и рабочих заметок: Установить Joplin.

Он будет вашим быстрым буфером, который затем можно будет структурировать и переносить в Wiki.js.

Эта комбинация покроет все ваши потребности и предоставит масштабируемую и мощную экосистему для управления информацией. Начать можно с установки Wiki.js — это даст вам самый главный инструмент для структурирования накопленных знаний.
"

Прошу дать инструкции по разворачиванию этих продуктов. Желательно с использованием docker контейнеров.

-------------------------------------------------

Мы будем разворачивать три сервиса: Wiki.js, OpenProject и Joplin. Однако Joplin — это десктопное приложение, которое не разворачивается через Docker для использования в браузере. Joplin имеет версию для рабочего стола (скачивается и устанавливается) и серверную часть для синхронизации, но так как мы фокусируемся на развертывании сервисов через Docker, то рассмотрим только Wiki.js и OpenProject.

Для Joplin, если вы хотите синхронизацию между устройствами, можно использовать их облачный сервис или развернуть свой сервер Joplin. Но поскольку в задаче упор на Docker, и Joplin Server не является обязательным для базового использования (можно использовать без сервера, синхронизируя через Dropbox, OneDrive и т.д.), мы сосредоточимся на Wiki.js и OpenProject.

Предположим, что вы хотите развернуть оба сервиса на одном сервере с помощью Docker Compose.

1.  Wiki.js с использованием Docker
    
2.  OpenProject с использованием Docker
    

Создадим docker-compose.yml файл, который запустит оба сервиса.

Однако учтите, что OpenProject требует довольно много ресурсов и имеет свой собственный способ развертывания через Docker. Мы будем использовать официальные образы.

Важно: Для production использования необходимо настроить обратный прокси (например, Nginx) и SSL. В данном примере мы просто запустим сервисы на разных портах.

Let's create a docker-compose.yml file:

version: '3'

services:  
wikijs:  
image: ghcr.io/requarks/wiki:2  
container_name: wikijs  
restart: unless-stopped  
environment:  
DB_TYPE: postgres  
DB_HOST: db_wikijs  
DB_PORT: 5432  
DB_USER: wiki  
DB_PASS: wiki_password  
DB_NAME: wiki  
ports:  
- "3000:3000"  
depends_on:  
- db_wikijs

db_wikijs:  
image: postgres:13  
container_name: db_wikijs  
restart: unless-stopped  
environment:  
POSTGRES_DB: wiki  
POSTGRES_USER: wiki  
POSTGRES_PASSWORD: wiki_password  
volumes:  
- db_wikijs_data:/var/lib/postgresql/data

openproject:  
image: openproject/community:12  
container_name: openproject  
restart: unless-stopped  
environment:  
OPENPROJECT_HOST__NAME: localhost:8080  
OPENPROJECT_SECRET_KEY_BASE: secret  
ports:  
- "8080:80"  
volumes:  
- op_assets:/var/openproject/assets  
- op_db_data:/var/openproject/db  
depends_on:  
- db_openproject

db_openproject:  
image: postgres:13  
container_name: db_openproject  
restart: unless-stopped  
environment:  
POSTGRES_DB: openproject  
POSTGRES_USER: openproject  
POSTGRES_PASSWORD: openproject_password  
volumes:  
- db_openproject_data:/var/lib/postgresql/data

volumes:  
db_wikijs_data:  
op_assets:  
op_db_data:  
db_openproject_data:

Но обратите внимание: OpenProject по умолчанию использует встроенную базу данных, но мы для надежности выносим в отдельный контейнер с Postgres.

Однако в официальной документации OpenProject для Docker рекомендуется использовать их образ с встроенной базой, но для production лучше отдельная база. Мы следуем примеру выше.

Теперь объяснение:

-   Wiki.js будет доступен на порту 3000, а OpenProject на порту 8080.
    
-   Для Wiki.js мы используем отдельный контейнер с Postgres.
    
-   Для OpenProject также используем отдельный контейнер с Postgres.
    

Важно: В среде OpenProject переменные для подключения к базе данных не заданы в нашем примере, потому что образ OpenProject по умолчанию использует встроенную базу, но мы хотим использовать внешнюю. Поэтому нам нужно передать переменные окружения для подключения к базе данных.

Уточним конфигурацию OpenProject:

openproject:  
image: openproject/community:12  
container_name: openproject  
restart: unless-stopped  
environment:  
OPENPROJECT_HOST__NAME: localhost:8080  
OPENPROJECT_SECRET_KEY_BASE: secret  
OPENPROJECT_DATABASE_URL: postgresql://openproject:openproject_password@db_openproject:5432/openproject  
ports:  
- "8080:80"  
depends_on:  
- db_openproject

Однако, согласно документации OpenProject, они используют переменные окружения для базы данных в формате:  
DATABASE_URL: postgresql://user:password@host:port/database

Но также можно использовать отдельные переменные:  
OPENPROJECT_DB_TYPE: postgres  
OPENPROJECT_DB_HOST: db_openproject  
OPENPROJECT_DB_PORT: 5432  
OPENPROJECT_DB_USER: openproject  
OPENPROJECT_DB_PASS: openproject_password  
OPENPROJECT_DB_NAME: openproject

Давайте используем отдельные переменные, так как это более наглядно.

Также обратите внимание, что для production необходимо задать надежный SECRET_KEY_BASE.

Итоговый docker-compose.yml:

version: '3'

services:  
wikijs:  
image: ghcr.io/requarks/wiki:2  
container_name: wikijs  
restart: unless-stopped  
environment:  
DB_TYPE: postgres  
DB_HOST: db_wikijs  
DB_PORT: 5432  
DB_USER: wiki  
DB_PASS: wiki_password  
DB_NAME: wiki  
ports:  
- "3000:3000"  
depends_on:  
- db_wikijs

db_wikijs:  
image: postgres:13  
container_name: db_wikijs  
restart: unless-stopped  
environment:  
POSTGRES_DB: wiki  
POSTGRES_USER: wiki  
POSTGRES_PASSWORD: wiki_password  
volumes:  
- db_wikijs_data:/var/lib/postgresql/data

openproject:  
image: openproject/community:12  
container_name: openproject  
restart: unless-stopped  
environment:  
OPENPROJECT_HOST__NAME: localhost:8080  
OPENPROJECT_SECRET_KEY_BASE: "your-secret-key-base-change-this-in-production"  
OPENPROJECT_DB_TYPE: postgres  
OPENPROJECT_DB_HOST: db_openproject  
OPENPROJECT_DB_PORT: 5432  
OPENPROJECT_DB_USER: openproject  
OPENPROJECT_DB_PASS: openproject_password  
OPENPROJECT_DB_NAME: openproject  
ports:  
- "8080:80"  
volumes:  
- op_assets:/var/openproject/assets  
- op_db_data:/var/openproject/db  
depends_on:  
- db_openproject

db_openproject:  
image: postgres:13  
container_name: db_openproject  
restart: unless-stopped  
environment:  
POSTGRES_DB: openproject  
POSTGRES_USER: openproject  
POSTGRES_PASSWORD: openproject_password  
volumes:  
- db_openproject_data:/var/lib/postgresql/data

volumes:  
db_wikijs_data:  
op_assets:  
op_db_data:  
db_openproject_data:

Теперь инструкция по запуску:

1.  Сохраните этот docker-compose.yml в папке.
    
2.  В той же папке выполните команду: `docker-compose up -d`
    

После запуска:

-   Wiki.js будет доступен по адресу http://localhost:3000
    
-   OpenProject будет доступен по адресу http://localhost:8080
    

При первом запуске Wiki.js потребует начальной настройки (создание администратора и т.д.). OpenProject также может потребовать время на инициализацию.

Для Joplin:

-   Скачайте и установите десктопное приложение с официального сайта: https://joplinapp.org/
    
-   Настройте синхронизацию, если нужно, через облачный сервис или свой сервер.
    

Если вы хотите использовать Joplin Server через Docker, то можно добавить в docker-compose.yml сервис для Joplin Server. Однако учтите, что это отдельная настройка.

Пример добавления Joplin Server (используется для синхронизации):

joplin_server:  
image: joplin/server:latest  
container_name: joplin_server  
restart: unless-stopped  
environment:  
APP_BASE_URL: http://localhost:22300  
DB_CLIENT: pg  
POSTGRES_DATABASE: joplin  
POSTGRES_USER: joplin  
POSTGRES_PASSWORD: joplin_password  
POSTGRES_PORT: 5432  
POSTGRES_HOST: db_joplin  
ports:  
- "22300:22300"  
depends_on:  
- db_joplin

db_joplin:  
image: postgres:13  
container_name: db_joplin  
restart: unless-stopped  
environment:  
POSTGRES_DB: joplin  
POSTGRES_USER: joplin  
POSTGRES_PASSWORD: joplin_password  
volumes:  
- db_joplin_data:/var/lib/postgresql/data

И добавить том db_joplin_data в раздел volumes.

Тогда в приложении Joplin укажите сервер синхронизации: http://localhost:22300 и создайте учетную запись.

Однако, это усложнит систему, так как у нас уже есть три базы данных. Убедитесь, что у вас достаточно ресурсов.

Внимание: В production необходимо настроить доменные имена, SSL и обратный прокси (например, Nginx) для доступа к сервисам по нормальным URL.

Надеюсь, это поможет вам начать!
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTEwMjU2ODAwNTRdfQ==
-->